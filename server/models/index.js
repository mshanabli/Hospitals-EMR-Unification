const { Hospital } = require('./hospital');
const { Column } = require('./column');
const { Relation } = require('./relation');
const { Patient } = require('./patient');

module.exports = { Hospital, Column, Relation, Patient };
